import { TThemeSizeScale } from './types';

export const sizeScale: TThemeSizeScale = {
  xs: 0.65,
  sm: 0.8,
  md: 1,
  lg: 1.2,
  xl: 1.4,
};

export type { TThemeSizeScale, TThemeSizeScaleKey } from './types';
