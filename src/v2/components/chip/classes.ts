import { generateUtilityClasses } from '../../../helpers/generate-utility-classes';

export const chipClasses = generateUtilityClasses('Chip', [
  'root',
  'clickable',
  'deletable',
  'delete',
  'disabled',
]);
