import { generateUtilityClasses } from '../../../../helpers/generate-utility-classes';

export const inputActionsClasses = generateUtilityClasses('InputActions', [
  'root',
  'horizontal',
  'vertical',
]);
