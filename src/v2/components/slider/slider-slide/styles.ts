import styled from '@emotion/styled';

export const SSliderSlide = styled.div`
  position: relative;
  box-sizing: border-box;
  flex: 0 0 var(--cui-slider-frame, 100%);
  width: var(--cui-slider-frame, 100%);
  min-width: var(--cui-slider-frame, 100%);
  max-width: var(--cui-slider-frame, 100%);
`;
