import { generateUtilityClasses } from '../../../../helpers/generate-utility-classes';

export const slidePermanentContentClasses = generateUtilityClasses(
  'V2SlidePermanentContent',
  ['root'],
);
