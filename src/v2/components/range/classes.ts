import { generateUtilityClasses } from '../../../helpers/generate-utility-classes';

export const rangeClasses = generateUtilityClasses('Range', [
  'root',
  'disabled',
  'error',
  'required',
]);
