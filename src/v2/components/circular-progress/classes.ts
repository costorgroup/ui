import { generateUtilityClasses } from '../../../helpers/generate-utility-classes';

export const circularProgressClasses = generateUtilityClasses('CircularProgress', [
  'root',
  'track',
  'path',
]);
