import styled from '@emotion/styled';

export const SBreadcrumbEllipsis = styled.span`
  display: inline-flex;
  flex-shrink: 0;
  align-items: center;
  justify-content: center;
  color: inherit;
  line-height: 0;
  vertical-align: middle;
  pointer-events: none;
`;
