import styled from '@emotion/styled';

export const SBreadcrumbIcon = styled.span`
  display: inline-flex;
  flex-shrink: 0;
  align-items: center;
  justify-content: center;
  color: inherit;
  line-height: 0;

  & > svg {
    display: block;
    width: 1.25em;
    height: 1.25em;
  }
`;
