import { generateUtilityClasses } from '../../../helpers/generate-utility-classes';

export const dockClasses = generateUtilityClasses('V2Dock', [
  'root',
  'subtle',
  'surface',
  'plain',
  'horizontal',
  'vertical',
  'opaque',
  'transparent',
]);
