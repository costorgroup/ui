import { generateUtilityClasses } from '../../../helpers/generate-utility-classes';

export const toggleButtonGroupClasses = generateUtilityClasses(
  'V2ToggleButtonGroup',
  ['root', 'horizontal', 'vertical', 'disabled'],
);
