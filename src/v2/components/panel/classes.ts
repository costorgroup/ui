import { generateUtilityClasses } from '../../../helpers/generate-utility-classes';

export const panelClasses = generateUtilityClasses('V2Panel', [
  'root',
  'subtle',
  'surface',
  'outline',
]);
