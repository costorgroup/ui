import { generateUtilityClasses } from '../../helpers/generate-utility-classes';

export const textAreaClasses = generateUtilityClasses('TextArea', [
  'root',
  'disabled',
  'error',
  'required',
]);
