import { generateUtilityClasses } from '../../../helpers/generate-utility-classes';

export const inputLabelClasses = generateUtilityClasses('InputLabel', [
  'root',
  'asterisk',
  'disabled',
  'error',
  'required',
]);
