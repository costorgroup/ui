import { generateUtilityClasses } from '../../../helpers/generate-utility-classes';

export const inputRadioButtonClasses = generateUtilityClasses(
  'InputRadioButton',
  ['root', 'input', 'control', 'dot', 'disabled', 'checked', 'error'],
);
