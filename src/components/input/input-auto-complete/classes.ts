import { generateUtilityClasses } from '../../../helpers/generate-utility-classes';

export const inputAutoCompleteClasses = generateUtilityClasses('InputAutoComplete', [
  'root',
  'disabled',
  'expanded',
  'open',
]);
