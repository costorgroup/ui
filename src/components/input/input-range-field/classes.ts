import { generateUtilityClasses } from '../../../helpers/generate-utility-classes';

export const inputRangeFieldClasses = generateUtilityClasses('InputRangeField', [
  'root',
  'active',
  'disabled',
]);
