import { generateUtilityClasses } from '../../../helpers/generate-utility-classes';

export const inputPinFieldClasses = generateUtilityClasses('InputPinField', [
  'root',
  'disabled',
  'readOnly',
]);
