import { generateUtilityClasses } from '../../../helpers/generate-utility-classes';

export const inputColorFieldClasses = generateUtilityClasses('InputColorField', [
  'root',
  'disabled',
  'expanded',
  'open',
]);
