import { generateUtilityClasses } from '../../../helpers/generate-utility-classes';

export const inputRichTextFieldClasses = generateUtilityClasses('InputRichTextField', [
  'root',
  'disabled',
]);
