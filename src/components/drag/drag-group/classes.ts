import { generateUtilityClasses } from '../../../helpers/generate-utility-classes';

export const dragGroupClasses = generateUtilityClasses('DragGroup', [
  'root',
  'placeholder',
  'dragging',
]);
