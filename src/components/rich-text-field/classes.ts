import { generateUtilityClasses } from '../../helpers/generate-utility-classes';

export const richTextFieldClasses = generateUtilityClasses('RichTextField', [
  'root',
  'disabled',
  'error',
  'required',
]);
