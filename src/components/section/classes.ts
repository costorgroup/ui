import { generateUtilityClasses } from '../../helpers/generate-utility-classes';

export const sectionClasses = generateUtilityClasses('Section', [
  'root',
  'alignLeft',
  'alignCenter',
  'alignRight',
]);
