import { generateUtilityClasses } from '../../../helpers/generate-utility-classes';

export const paginationItemClasses = generateUtilityClasses('PaginationItem', [
  'root',
  'disabled',
  'selected',
]);
