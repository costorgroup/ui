import styled from '@emotion/styled';

export const STreeViewIcon = styled.span`
  display: inline-flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  color: inherit;

  svg {
    width: 1.15em;
    height: 1.15em;
  }
`;
