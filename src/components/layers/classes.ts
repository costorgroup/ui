import { generateUtilityClasses } from '../../helpers/generate-utility-classes';

export const layersClasses = generateUtilityClasses('Layers', [
  'root',
  'scene',
  'spreadTop',
  'spreadRight',
  'spreadBottom',
  'spreadLeft',
]);
