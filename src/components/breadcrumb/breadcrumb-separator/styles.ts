import styled from '@emotion/styled';

export const SBreadcrumbSeparator = styled.li`
  display: inline-flex;
  align-items: center;
  color: var(--breadcrumb-muted);
  pointer-events: none;
  user-select: none;
`;
