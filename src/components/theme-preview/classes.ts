import { generateUtilityClasses } from '../../helpers/generate-utility-classes';

export const themePreviewClasses = generateUtilityClasses('ThemePreview', [
  'root',
  'chrome',
  'dot',
  'sidebar',
  'content',
  'bar',
  'panel',
]);
