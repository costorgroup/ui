import styled from '@emotion/styled';
import { Panel } from '../panel';

export const SViewport = styled(Panel)`
  position: relative;
  display: block;
  min-width: 200px;
  min-height: 150px;
  overflow: hidden;
  transform: translateZ(0);
`;
