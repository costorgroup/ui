import { generateUtilityClasses } from '../../../helpers/generate-utility-classes';

export const flipbookPageClasses = generateUtilityClasses('FlipbookPage', [
  'root',
  'front',
  'back',
  'hard',
]);
