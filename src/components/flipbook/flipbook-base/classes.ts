import { generateUtilityClasses } from '../../../helpers/generate-utility-classes';

export const flipbookBaseClasses = generateUtilityClasses('FlipbookBase', [
  'root',
  'stage',
  'book',
  'controls',
  'action',
  'status',
]);
