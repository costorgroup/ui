import styled from '@emotion/styled';

export const SStepStatus = styled.span`
  display: inline-flex;
  align-items: center;
  justify-content: center;
  line-height: 0;
  color: inherit;

  & > svg {
    width: 0.7em;
    height: 0.7em;
  }
`;
