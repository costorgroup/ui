import { generateUtilityClasses } from '../../helpers/generate-utility-classes';

export const stepperClasses = generateUtilityClasses('Stepper', [
  'root',
  'horizontal',
  'vertical',
  'alternativeLabel',
]);
