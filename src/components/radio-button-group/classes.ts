import { generateUtilityClasses } from '../../helpers/generate-utility-classes';

export const radioButtonGroupClasses = generateUtilityClasses('RadioButtonGroup', [
  'root',
  'disabled',
  'error',
]);
