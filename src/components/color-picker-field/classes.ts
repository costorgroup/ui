import { generateUtilityClasses } from '../../helpers/generate-utility-classes';

export const colorPickerFieldClasses = generateUtilityClasses('ColorPickerField', [
  'root',
  'disabled',
  'error',
  'required',
]);
