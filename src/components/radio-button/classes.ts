import { generateUtilityClasses } from '../../helpers/generate-utility-classes';

export const radioButtonClasses = generateUtilityClasses('RadioButton', [
  'root',
  'checked',
  'disabled',
  'error',
]);
