import { generateUtilityClasses } from '../../helpers/generate-utility-classes';

export const chromaConicSpinClasses = generateUtilityClasses('ChromaConicSpin', [
  'root',
  'ring',
  'content',
]);
